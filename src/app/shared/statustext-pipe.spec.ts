import { StatustextPipe } from './statustext-pipe';

describe('StatustextPipe', () => {
  it('create an instance', () => {
    const pipe = new StatustextPipe();
    expect(pipe).toBeTruthy();
  });
});
